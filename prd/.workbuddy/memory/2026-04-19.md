# 2026-04-19 工作记录

## PRD 更新

- NBCoder PRD 更新至 V1.2
- 新增 §2.5 AI Provider 配置章节，包含：
  - 多 Provider 管理（OpenAI/Claude/本地模型）
  - 多模型支持（每个 Provider 多个模型变体）
  - Agent 模型链配置界面
  - 运行时模型选择与降级策略
  - YAML 配置文件格式
- 新增 §13.4 AI Provider 表结构（逻辑结构，存储在 YAML）
- 术语表新增 AI Provider、Agent 模型链术语

## 产品 Agent 对话记忆

- 采用 OpenClaw 三层记忆模型（Context → Compaction → Memory Files）
- 新增 §6.6 产品 Agent 对话记忆章节
- Compaction 触发条件：token > context_window - 20000 - 4000
- Memory Files 结构：MEMORY.md（长期）+ memory/YYYY-MM-DD.md（每日）
- 支持手动命令：/compact, /memory, /memory flush
- 术语表新增：三层记忆模型、Compaction、Memory Flush 等术语

## 产品 Agent 定位更新

- PRD 更新至 V1.3
- 产品 Agent 明确定位为"受限的 OpenClaw"
- 工具限制：仅能读写产品文档目录（knowledge-base/cards/）
- 能力边界：禁止代码执行、Git 操作、Agent 调度
- 核心输出：待决策的需求卡片（草稿状态）
- §6.1 新增产品 Agent 定位说明
- §6.4 工具白名单新增产品 Agent 特殊限制

## V1.4 架构决策

- 子 Agent 封装为 Skill，以 tool 方式调用
- 入口 Agent 由任务调度器轮询任务表触发
- 无实时性要求，采用批量处理模式
- §6.2 新增架构图和调度器设计
- §6.2.1 新增任务调度器详细设计（轮询循环、配置）
- §6.2.2 新增 Skill 封装规范
- 术语表新增 Skill、任务调度器、轮询间隔术语

## V1.5 架构决策

- 系统仅发起 PR/MR，不执行合并操作
- 合并由人工完成（GitHub PR / GitLab MR）
- 系统不删除开发分支
- §5.2 阶段⑦更新描述
- §12.3 安全性更新
- 分支清理（P2）决策：不做，系统保留分支

## V1.6 服务生命周期管理

- 新增 §12.4 服务生命周期管理章节
- §12.4.0 新增崩溃恢复机制（启动时自动修复）
  - 检测 running → interrupted 状态转换
  - 克隆 dirty 状态处理：尝试还原或通知用户
  - WebSocket 通知用户崩溃影响范围
- §12.2 可靠性新增崩溃检测与告警机制（健康检查、心跳日志、外部监控）
- 术语表新增：崩溃恢复、dirty 克隆

## V1.7 Git 提交策略

- 开发中采用增量 commit：每个有意义的变更完成后立即 commit + push
- Commit 格式：`type(req-id): 简短描述`（开发中）
- PR 合并时可选 squash：保持主干分支整洁
- 最终合并格式：`type(REQ-id): 功能描述`
- §8.3 规范示例新增 Git 提交规范章节

## V1.8 DDD 领域拆分

- §2.6 新增 DDD 领域拆分章节
- 核心域：需求领域、流水线领域、智能体领域
- 支撑域：知识库领域、克隆池领域、AI 运行时
- 通用域：项目领域、Git 领域、通知领域
- 限界上下文：8 个上下文独立模型
- 上下文映射：需求→流水线→智能体→克隆池/AI
- 聚合设计：CardAggregate、TaskAggregate
- 领域事件：15+ 事件用于解耦和追踪

## V1.8 精简 PRD 内容

- 精简 §2.5 AI Provider 配置章节
- 移除详细的 UI 界面 ASCII 图（2.5.2 配置界面、2.5.3 Provider 编辑弹窗）
- 移除详细的 YAML 配置文件示例（2.5.6 配置存储）
- 保留：功能概述、Provider/模型配置项、Agent 模型链、降级策略
- PRD 版本更新至 V1.8
